# Git & Vercel Deployment Rules
- **Pre-commit Check:** Verify metadata title in `src/app/layout.tsx` is set to "Raggio Gourmet & Pizza | Newark, DE".
- **Clean Structure:** Ensure no nested `.git` folders or broken git submodule references exist.
- **Vercel Sync:** Always test that menu PDFs and static links resolve properly before final deployment.

